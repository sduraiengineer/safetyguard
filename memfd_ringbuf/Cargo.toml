[package]
name = "memfd_ringbuf"
version = "0.1.0"
edition = "2024"

[dependencies]
ringbuf = { version = "0.5.0", features = []}
memmap2 = "0.9.10"
rustix = { version = "1.1.4", features = [ "fs", "mm"] }
bytemuck = { version = "1.25.0", features = ["derive"] }
